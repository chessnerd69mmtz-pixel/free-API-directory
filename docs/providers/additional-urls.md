# Additional Provider URLs

This file contains 0 unique provider URLs extracted from the supplied source list. Providers already present in `data/providers.json`, duplicate provider names, and duplicate URLs were excluded.

**Important:** this is a URL catalog only. It intentionally does not assert free-tier size, credit-card requirements, expiry, or verification status for these additions.

| Provider | Category | URL |
|---|---|---|
